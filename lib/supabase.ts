/**
 * SafeDrive SIH26037 - Supabase Client Configuration
 * 
 * To use Supabase in this Next.js project:
 * 1. Run: npm install @supabase/supabase-js
 * 2. Add NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY to your .env.local file.
 */

import { Database } from "@/types/supabase";

// Optional: uncomment once @supabase/supabase-js is installed
// import { createClient } from "@supabase/supabase-js";
//
// const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || "";
// const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || "";
//
// export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey);
